import FormsPage from './pages/FormsPage.jsx';

function App() {
  // As the site grows, this is where you'd add routing
  // (e.g. react-router) between multiple pages. For now
  // there's just the one page.
  return <FormsPage />;
}

export default App;
